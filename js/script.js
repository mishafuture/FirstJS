'use strict';

let options = {
    title: "menu",
    size: {
        width: 100,
        height: 200
    }
};

const {title, size: {width, height}} = options;

console.log(title);
console.log(width);